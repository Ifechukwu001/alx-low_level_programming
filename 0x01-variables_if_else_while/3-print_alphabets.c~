#include <stdio.h>

/**
 * main - outputs a character in the alphabet (lowercase).
 *
 * Return: Always 0 if successful.
 */
int main(void)
{
	char i = 'a';

	while (i != '{')
	{
		putchar(i);
		i++;
	}

	char j = 'A';

	while (j != '[')
	{
		putchar(j);
		j++;
	}
	putchar('\n');
	return (0);
}
